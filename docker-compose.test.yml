# =============================================================================
# docker-compose for TEST / QA (Sepolia)
# Usage: docker compose -f docker-compose.test.yml up -d
# URL:   http://localhost:8080
# =============================================================================

services:
  voting-app:
    image: ghcr.io/${GITHUB_REPOSITORY:-stunning-disco}:test
    ports:
      - "8080:8080"
    restart: unless-stopped
